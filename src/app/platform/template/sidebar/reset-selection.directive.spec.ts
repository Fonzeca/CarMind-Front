import { ResetSelectionDirective } from './reset-selection.directive';

describe('ResetSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
